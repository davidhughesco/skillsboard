<div class="row">
  <div class="col-md-12">
    <%= link_to 'Edit', edit_course_path(@course), class: "btn btn-default" %> <%= link_to 'Back', courses_path, class: "btn btn-primary" %>
  </div>
</div>

<h1><%= @course.title %></h1>
<%= sanitize (@course.content) %>
